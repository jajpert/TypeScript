const first_name = 'Julia';
const another_name = 'Ajpert';
const x: number = 1;

function greeting(name, sobrenome) {
    console.log(`Olá ${name} ${sobrenome}`)
}

greeting(first_name, another_name)